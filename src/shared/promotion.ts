// units that are running on a special
//support to show on the home page

export interface Promotion {
    id: number;
    name: string;
    image: string;
    label: string;
    price: string;
    featured: boolean;
    description: string;
}